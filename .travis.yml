language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.5-dev"
  - "3.6-dev"
  - "nightly"

install:
  - pip install -U pytest pytest-cov
  - pip install .
script:
  - pytest --cov journalwatch test_journalwatch.py
after_success:
  - bash <(curl -s https://codecov.io/bash)
